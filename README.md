# FIFO
**FIFO (First-In, First-Out)** is a memory structure in which the data entered first is the first data to be read or removed. This project demonstrates FIFO operations such as **write, read, full, and empty** status using sequential logic.
